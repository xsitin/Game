# План миграции с WinForms на Godot

## Что сделано в этом коммите
- Создан каркас отдельного Godot-клиента в `GodotClient/`.
- Добавлена стартовая сцена `Main.tscn` и C#-скрипт `Main.cs`.
- Зафиксирован поэтапный план переноса, чтобы мигрировать инкрементально.

## Пошаговая стратегия
1. **Отвязать логику от WinForms**
   - вынести из `Game/Model/Game.cs` вызовы `MessageBox` и `Application.OpenForms` в интерфейсы;
   - оставить в модели только правила боя и смену состояний.
2. **Собрать pure-core слой**
   - сгруппировать текущие `Game/Model/*` в отдельную библиотеку `Game.Core` без `System.Windows.Forms`.
3. **Поднять сцену боя в Godot**
   - отдельная сцена `CombatScene`;
   - отрисовка команд и очереди хода из данных core-слоя.
4. **Перенести UI-экраны**
   - меню, инвентарь, прокачка, магазин.
5. **Сохранения и совместимость**
   - подключить существующий формат сейвов;
   - добавить версионирование формата.

## Ближайший технический backlog
- [ ] Создать интерфейсы `IGameNotifications` и `IGameViewRefresher` в модели.
- [ ] Убрать прямые зависимости от `System.Windows.Forms` из core-логики.
- [ ] Подключить `Game.Core` как зависимость Godot-проекта.
- [ ] Реализовать первый playable vertical slice (1 бой + экран результата).
